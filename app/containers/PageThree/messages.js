/*
 * PageThree Messages
 *
 * This contains all the text for the PageThree container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PageThree';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PageThree container!',
  },
});

/*
 * PageTwo Messages
 *
 * This contains all the text for the PageTwo container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PageTwo';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PageTwo container!',
  },
});

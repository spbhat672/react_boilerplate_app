/*
 * PageOne Messages
 *
 * This contains all the text for the PageOne container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PageOne';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PageOne container!',
  },
});
